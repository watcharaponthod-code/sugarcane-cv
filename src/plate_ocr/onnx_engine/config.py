# -*- coding: utf-8 -*-
"""ค่าตั้งต้นทั้งหมดของระบบอ่านป้ายทะเบียน — ปรับจูนที่ไฟล์นี้ไฟล์เดียว"""

# ---------- ขนาดภาพ ----------
PROC_WIDTH = 1280          # ย่อภาพให้กว้างไม่เกินนี้ตอน "หาตำแหน่งป้าย" (OCR ใช้ภาพความละเอียดเต็ม)

# ---------- เงื่อนไขรูปทรงป้าย (ใช้กรอง candidate) ----------
ASPECT_MIN = 1.4           # กว้าง/สูง ของป้ายไทยมาตรฐาน ~2.2 (เผื่อมุมกล้องเอียง)
ASPECT_MAX = 6.0
AREA_MIN_RATIO = 0.0008    # พื้นที่ป้ายขั้นต่ำ เทียบกับพื้นที่ทั้งภาพ
AREA_MAX_RATIO = 0.12      # พื้นที่ป้ายสูงสุด เทียบกับพื้นที่ทั้งภาพ
PLATE_MIN_WIDTH = 70       # px ขั้นต่ำ (บนภาพที่ย่อแล้ว) — เล็กกว่านี้ OCR อ่านไม่ไหว
MAX_CANDIDATES = 7         # ส่งป้ายเข้า OCR สูงสุดกี่ใบต่อภาพ

# ---------- morphology (วิธีที่ 1) ----------
BLACKHAT_KERNEL = (13, 5)  # เคอร์เนลเน้น "ตัวอักษรเข้มบนพื้นสว่าง" (กว้าง x สูง)
LIGHT_KERNEL = (3, 3)

# ---------- OCR ----------
OCR_ENGINE = "fast"        # "fast" (ONNX เฉพาะทาง - แม่นสุด) | "template" (CV ล้วน) |
                           # "easyocr" | "tesseract" | "auto"
OCR_ALLOWLIST = None       # จำกัดชุดตัวอักษรของ easyocr เช่น "0123456789-" สำหรับป้ายรถบรรทุก
                           # (None = ไม่จำกัด อ่านจังหวัดได้ด้วย)
OCR_PLATE_HEIGHT = 200     # ขยายภาพป้ายให้สูงเท่านี้ (px) ก่อนอ่าน
OCR_MIN_CONF = 0.25        # ตัดผลอ่านที่ความมั่นใจต่ำกว่านี้ทิ้ง
CROP_MARGIN = 0.07         # เผื่อขอบตอน crop ป้าย (สัดส่วนของความกว้างป้าย)

# ---------- โหมดวิดีโอ / RTSP ----------
VIDEO_FRAME_SKIP = 5       # ประมวลผลทุก ๆ N เฟรม (ลดโหลด CPU)
DEDUP_SECONDS = 8          # ทะเบียนเดิมโผล่ซ้ำภายใน N วินาที ไม่บันทึกซ้ำ

# ---------- การกรองผลลัพธ์ ----------
MIN_NUMBER_DIGITS = 2      # เลขทะเบียนต้องมีตัวเลขอย่างน้อยกี่หลัก (กันอ่านขยะ)
FALLBACK_FULL_IMAGE = True   # หาป้ายไม่เจอ -> ลอง OCR ทั้งภาพ (เฉพาะภาพทรงป้าย)
WEAK_RESULT_MIN_CONF = 0.35  # ผลที่ "ไม่มีทั้งขีด(-)และจังหวัด" ต้องมั่นใจอย่างน้อยเท่านี้
OCR_MAX_HEIGHT = 420         # ภาพเข้า OCR สูงสุด (กันกินแรม/OOM)
EASYOCR_CANVAS = 1280        # จำกัด canvas ของ EasyOCR detector
EARLY_STOP_ON_STRONG = True  # เจอป้ายหลักฐานแน่น (ขีด/จังหวัด) แล้วหยุด OCR ใบอื่น

# ---------- โหมดลานชั่ง ----------
SINGLE_PLATE_MODE = True   # คืน "ป้ายที่ดีที่สุดใบเดียว" (รถผ่านทีละคัน)
POSITION_PRIOR = True      # ป้ายจริงอยู่ต่ำ/กลางลำตัวรถ ส่วนสติกเกอร์อยู่บนกระจก
VOTE_MIN_FRAMES = 3        # โหมดวิดีโอ: ต้องอ่านตรงกันอย่างน้อยกี่เฟรมก่อนยืนยันผล
